# Build satu berkas HTML lalu terbitkan ke GitHub Pages.
# Aktifkan dulu: Settings → Pages → Source = "GitHub Actions".
name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # `npm install`, bukan `npm ci`: package-lock.json masih mencantumkan
      # concurrently yang sudah dihapus dari package.json, dan `npm ci` menolak
      # jalan kalau keduanya tidak cocok.
      - run: npm install --no-audit --no-fund

      - run: node scripts/build-gh-pages.mjs

      - uses: actions/upload-pages-artifact@v3
        with:
          path: gh-pages

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
