# PostgreSQL connection. Adjust user/password/host to your local install.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kneefit3d
PORT=4000

# ML service (FastAPI). Backend falls back to stub if unreachable.
ML_SERVICE_URL=http://localhost:8000
